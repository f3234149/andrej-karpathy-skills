# AGENTS.md

本文件用于指导 Codex 和其他代码智能体在本仓库中工作。

## 项目概览

在这里补充仓库的基本信息：

- 项目用途：TODO
- 主要语言/框架：TODO
- 应用入口：TODO
- 重要目录：
  - `src/`：应用源码
  - `tests/`：自动化测试
  - `docs/`：项目文档

如果实际仓库结构与上面的示例不同，请以现有代码为准，并及时更新本文件。

## 环境准备

使用项目约定的包管理器安装依赖。

```bash
# TODO: 替换为本项目正确的安装命令
npm install
```

如果项目需要环境变量，请在 `.env.example` 中说明。不要提交真实密钥、令牌、API Key、私有证书或本地 `.env` 文件。

## 常用命令

优先使用以下命令；如果项目使用其他工具链，请替换为实际命令。

```bash
# 启动本地开发环境
npm run dev

# 运行测试
npm test

# 运行代码检查
npm run lint

# 构建生产版本
npm run build
```

在汇报代码修改已完成之前，应运行最相关的验证命令。如果某个命令无法运行，需要说明原因，并指出剩余风险。

## 工作规则

- 修改前先阅读相关上下文代码。
- 变更范围应聚焦于用户请求。
- 优先沿用仓库中已有的模式、工具函数、组件和抽象。
- 不要随意引入新依赖；确有必要时，应说明原因。
- 不要因为个人风格偏好重写无关代码。
- 保留工作区中已有的用户改动。
- 除非生成源不可用且用户明确要求，否则不要手动编辑生成文件。
- 未经用户要求，不要提交、推送、发布、部署，也不要运行破坏性命令。

## 代码风格

- 遵循正在编辑文件的既有风格。
- 使用清晰的命名，避免过度缩写。
- 保持函数易读，避免把过多职责塞进同一个函数。
- 只在解释非显而易见的意图或约束时添加注释。
- 优先使用类型化、结构化的数据处理方式，避免脆弱的字符串拼接和解析。
- 除非用户请求需要，否则保持公开 API 的兼容性。

## 测试要求

- 行为发生变化时，应新增或更新测试。
- 测试应尽量靠近被覆盖的代码或功能。
- 优先测试可观察行为，而不是内部实现细节。
- 修复缺陷时，条件允许应添加回归测试。
- 涉及 UI 改动时，应检查关键响应式状态，避免文字溢出、遮挡或布局重叠。

## 架构边界

在这里记录本项目特有的架构约束。示例：

- UI 组件不应直接调用外部服务。
- 数据库访问应通过仓库约定的数据访问层完成。
- 共享校验 schema 应放在统一模块中维护。
- 业务逻辑应尽量避免散落在路由或控制器胶水代码中。

如果暂时没有明确文档，请从现有代码中推断边界，并保持修改与现有架构一致。

## 安全与风险控制

- 将所有用户输入视为不可信。
- 不要记录密钥或敏感个人信息。
- 在系统边界处进行数据校验。
- 能用更小权限完成时，不要使用宽泛的文件系统、网络或 Shell 访问。
- 运行迁移、删除数据、轮换凭据或修改生产配置前，必须先征得用户确认。

## 文档维护

以下内容变化时，应同步更新相关文档：

- 环境准备步骤
- 公开 API
- 用户可见行为
- 配置项
- 运维或开发命令

文档应简洁、准确。优先写具体命令和路径，少写泛泛建议。

## 完成检查清单

结束工作前确认：

- 用户请求的修改已经完成。
- 已运行相关测试、代码检查或构建；如果没有运行，已说明原因。
- 必要的用户可见行为变更已经记录到文档。
- 没有故意修改无关文件。
- 已清楚说明剩余风险或后续工作。

